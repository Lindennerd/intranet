<div class="row">
    <div class="col s12">

        <form action="/users/add" method="POST" class="card">
            <div class="card-content row">
                <div class="card-title">
                    Novo Usuário
                </div>
                <div class="input-field col s12 m4">
                    <input type="text" name="name" required>
                    <label for="name">Nome</label>
                </div>
                <div class="input-field col s12 m3">
                    <input type="email" name="email" required>
                    <label for="email">Email</label>
                </div>
                <div class="input-field col s12 m4">
                    <input type="password" name="password" required>
                    <label for="password">password</label>
                </div>
            </div>
            <div class="card-action">
                <button class="btn waves-effect waves-light" type="submit">Enviar
                    <i class="material-icons right">send</i>
                </button>
            </div>
        </form>

    </div>
    {{#if error}}
    <div class="col s12">
        <div class="card-panel red">
            {{error}}
        </div>
    </div>
    {{/if}}
    <div class="col s12">
        <div class="card-panel">
            <table class="responsive-table">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Email</th>
                        <th>Administrador</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {{#each users}}
                    <tr>
                        <td>{{name}}</td>
                        <td>{{email}}</td>
                        <td>
                            {{#if isAdmin}} 
                                <i class="material-icons">done</i> 
                            {{else}}
                                <i class="material-icons">block</i>
                            {{/if}}
                        </td>
                        <td>
                            <a href="/users/delete/{{id}}" class="btn waves-effect waves-light red">Deletar</a>
                            {{#if isAdmin}} 
                                <a href="/users/changeadmin/{{id}}" class="btn waves-effect waves-light">Remover Admin</a>
                            {{else}}
                                <a href="/users/changeadmin/{{id}}" class="btn waves-effect waves-light">Tornar Admin</a>
                            {{/if}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>